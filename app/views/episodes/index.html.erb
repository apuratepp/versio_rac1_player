<h1>Versió Rac1</h1>

<h2>
  <%= link_to l(@episodes.first.previous_broadcasted_at, format: :long), @episodes.first.previous_broadcasted_at_params %>
  <%=l @episodes.first.broadcasted_at, format: :long %>
  <%= link_to l(@episodes.first.next_broadcasted_at, format: :long), @episodes.first.next_broadcasted_at_params %>
</h2>

<% @episodes.each do |episode| %>
  <div class="episode">
    <h3><%= episode.broadcasted_at.strftime("%R") %></h3>
    <audio controls preload=none src="<%= episode.link %>" type="audio/mpeg"></audio>
  </div>
<% end %>
